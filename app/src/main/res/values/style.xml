<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="colored_checkbox" parent="Theme.AppCompat.Light">
        <!-- selected checkboxColor -->
        <item name="colorAccent">@android:color/holo_purple</item>
        <!-- unselected checkbox color -->
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <!-- progress bar -->
    <style name="my_progress_bar" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@android:color/holo_purple</item>
    </style>

    <style name="black" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:colorPrimaryDark">@android:color/black</item>
        <item name="buttonStyle">@style/Widget.AppCompat.ActionBar.Solid</item>
    </style>
</resources>